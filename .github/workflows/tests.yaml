name: CI Testing
permissions:
  contents: read
  pull-requests: write

on:
  pull_request:
    branches:
    - main

jobs:
  test:
    name: "Python ${{ matrix.python }} Django ${{ matrix.django }}"
    timeout-minutes: 10
    runs-on: ubuntu-latest
    strategy:
      # max-parallel: 8  # default is max available
      fail-fast: false
      matrix:
        include:
          - django: "5.2"
            python: "3.10"
          - django: "5.2"
            python: "3.11"
          - django: "5.2"
            python: "3.12"
          - django: "5.2"
            python: "3.13"
          - django: "5.2"
            python: "3.14"

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install uv
        uses: astral-sh/setup-uv@v6
      - name: Install Python
        run: uv python install ${{ matrix.python }}

      - name: Install Packages
        run: |
          uv sync --python ${{ matrix.python }} --extra extended --no-default-groups

      - name: Run Tests
        run: |
          echo "Python ${{ matrix.python }} / Django ${{ matrix.django }}"
          uv run --no-sync pytest --cov-config=pyproject.toml --cov-report=term-missing
        continue-on-error: ${{ contains(matrix.django, 'b') }}  # allow failing on e.g. 'django: "5.2b1"'
